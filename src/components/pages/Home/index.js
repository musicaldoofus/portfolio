import React from 'react';
import Page from '../';
import './Home.css';

const Home = (props) => {
	// console.log('h', props);
	return (
		<Page className="home">
		</Page>
	);
}

export default Home;